/**
 * A function that logs messages from the graph interface
 * during the execution of the client.
 * @param message The message to log.
 * @returns void
 */
type GraphInterfaceLogger = (message: string) => void;

export default GraphInterfaceLogger;